<nav class="navbar navbar-expand-lg fixed-top mb-3 bg-white">
  <div class="container">
    <%= link_to current_user ? timeline_path : root_path, class: "navbar-brand" do %>
      <%= image_tag "logo.png", alt: "logo" %>
    <% end %>

    <span class="navbar-toggler" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <%= image_tag "menu.svg", alt: "menu icon" %>
    </span>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        <% if current_user %>
          <div class="dropdown">
            <button class="btn btn-link dropdown-toggle nav-link" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Hi, <%= current_user.username %>
            </button>

            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
              <%= link_to "All Users", users_path, class: "dropdown-item" %>
              <%= link_to "Friend Requests", pending_friend_requests_path, class: "dropdown-item" %>
              <div class="dropdown-divider"></div>
              <%= link_to "Account Settings", edit_user_registration_path, class: "dropdown-item" %>
              <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </div>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>

          <li class="nav-item">
            <%= link_to "Signup", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
